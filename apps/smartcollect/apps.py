from django.apps import AppConfig


class SmartcollectConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.smartcollect"
    label = "smartcollect"
    verbose_name = "SchoolOS Smart Money Collection - policy, batches and approval"
